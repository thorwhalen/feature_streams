Project 1: Core Refactoring and Signal Transformation FrameworkProject Name: INGRESS_REWIREAlternative Name Candidate: wide (Feature widening)Function: To refactor the core signal transformation logic of the theremin project into a modular, testable, and self-assembling data pipeline using meshed.slabs. This package is the Transformation Layer that converts raw input features (like those from a video stream) into clean, normalized control values for the synthesis engine.I. Agent Mandate and ToolsToolRole in ProjectJustificationmeshed.slabsCore orchestrationMust be used to define transformation stages (Slabs) and connect them via argument-name-based wiring. This fulfills the self-assembling component framework requirement.dolPersistence/StorageUsed to store and retrieve calibration constants (min/max feature values, scaling factors, offsets) derived from video analysis.qh.testingVerificationRequired for launching and tearing down stream pipelines for robust, reproducible feature testing.scipy.signalAlgorithmUsed for filtering and mathematical normalization steps within individual Slabs.II. Core Refactoring Plan (The wide Pipeline)The refactoring focuses on replacing monolithic transformation functions with a series of small, single-purpose functions (nodes) assembled into a meshed.slabs.Slab instance, defining the flow: Raw Video Features $\rightarrow$ Control Vector.Define Core Transformation Nodes (Functions): The AI agent must write Python functions for the following steps. Each function is a potential node in the meshed graph:get_raw_features(video_frame): Extracts primary features (e.g., hand X, Y positions, facial expression scores).normalize_feature(feature_val, min_val, max_val): Normalizes a raw feature to the $[0.0, 1.0]$ range using constants retrieved from dol.apply_smoothing(normalized_stream): Applies a low-pass filter (using scipy.signal) to reduce jitter.rewire_to_control_vector(l_hand_y, r_hand_x): Implements the core $f \to l\_f, r\_f$ generalization logic, mapping normalized features to canonical DSP controls (pitch_hz_control, volume_control).Slab Construction and Wiring:The agent must create a Slab using a list of these functions, relying only on argument-name-based wiring for component connection.All nodes requiring calibration constants (e.g., min_val, max_val for normalize_feature) must receive them dynamically from a source function connected via dol in the wrapper application.Video Feature Data Generation: The agent must create utility functions to process the provided test videos (theremin_test_1.mp4, etc.) into storable JSON feature streams.Mandate: Use the test videos to compute a variety of video features (hand distance, normalized coordinates, facial action units) and store them in the format: tests/testing_data/theremin_test_N__video_features.json.Data Contract (Feature JSON): A list of time-stamped dictionaries: [{'t': 0.0, 'l_hand_x': 500, 'r_hand_y': 200, 'face_smile_score': 0.9}, ...].III. Testing Plan and VerificationThe core refactoring must be verified using the computed feature streams.Unit Tests (Slab Verification): Test individual Slab functions (e.g., normalize_feature) with fixed inputs to ensure mathematical integrity.Integration Tests (Pipeline Fidelity):Procedure: Use a mock input stream generator that reads the precomputed theremin_test_N__video_features.json data and feeds it into the wide pipeline.Tool Usage: Use the provided qh.testing.AppRunner (or test_app alias) to launch a mock wide processing service.Verification: Verify that the output Control Vector stream (e.g., a stream of {'pitch_hz_control': 440, 'volume_control': 0.8}) matches a manually verified expected output file for the given test video. This ensures the full feature-to-control mapping is correct.Input Stream Projects (Creating Feature Streams)Project 2: MOUSE_TRACKERProject Name: MOUSE_TRACKERAlternative Name Candidate: xyFunction: Capture real-time mouse/trackpad movement, click, and Mac-specific trackpad scroll/pinch events, converting them into a high-frequency feature stream.ToolRole in ProjectRelevant PackagepynputCore I/OProvides cross-platform listeners for mouse/keyboard events.pyobjc-framework-AppKitMac TrackpadResearch bindings to capture high-resolution NS/NSEvent trackpad gestures (pinch, swipe) that pynput may miss.meshedOptional AdapterUsed to create a simple Slab that samples/downscales the high-frequency stream to a rate suitable for wide.Data Contract (Output Stream): A sequence of dictionaries (stream of dicts) published at a high rate (e.g., 60Hz).Python# Stream Dict Example
{
    'timestamp': float,          # System time of event capture
    'x_norm': float,             # Normalized X position (0.0 to 1.0 of screen width)
    'y_norm': float,             # Normalized Y position (0.0 to 1.0 of screen height)
    'left_click': bool,          # True if left button is currently down
    'scroll_delta_y': float,     # Trackpad vertical scroll value
    'pinch_magnification': float # Mac-specific pinch-to-zoom (delta from rest)
}
Testing Plan:Setup: Create a mock client that listens to the MOUSE_TRACKER service.Tool Usage: Use qh.testing.service_running to launch the stream service in a separate process.Verification: The AI agent must write a test that simulates a known sequence of movements (e.g., using pynput.mouse.Controller) and verifies that the output stream dicts have the correct x_norm, y_norm, and boolean states. Verify that the normalization logic maps screen coordinates to the desired $[0.0, 1.0]$ range.Project 3: KEY_STREAMERProject Name: KEY_STREAMERAlternative Name Candidate: hz (Keyboard inputs often map to fundamental frequencies)Function: Capture keyboard key presses and releases and convert them into a structured, time-stamped stream suitable for musical control (e.g., playing a sequence of notes or changing synth parameters).ToolRole in ProjectRelevant Packagepynput.keyboardCore I/OListener for key press and release events.python-midi / midoUtilitiesHelpful for converting key presses (e.g., 'A', 'S') into standard MIDI note numbers.Data Contract (Output Stream): A time-stamped sequence of key event dictionaries.Python# Stream Dict Example
{
    'timestamp': float,         # System time of event
    'key': str,                 # E.g., 'a', 'shift', 'ctrl_l'
    'event_type': str,          # 'press' or 'release'
    'midi_note': int or None,   # MIDI note number if key is mapped to a musical pitch (e.g., 60 for Middle C)
    'is_mod_key': bool          # True if key is a modifier (Shift, Ctrl, Alt)
}
Testing Plan:Dual-Stream Integration Test: The agent must demonstrate how to combine the KEY_STREAMER output with the video feature stream.Procedure: Create a combined test scenario:A mock KEY_STREAMER process (using qh.testing or a simple stream generator) that outputs {'key': 'a', 'event_type': 'press'}.A separate mock video feature stream generator that outputs a constant pitch/volume control vector.A meshed.Slab component within the wide framework that: If key='a' is pressed, then multiply pitch_hz_control by $2$ (octave shift).Verification: The test must verify that when the key press is injected, the final output control stream reflects the expected octave shift, proving the combinatorial logic works.Project 4: AUDIO_FEATURERProject Name: AUDIO_FEATURERAlternative Name Candidate: khzFunction: Use a live microphone/audio input to extract musical features (pitch, loudness, spectral centroid) in real-time and output them as a control stream.ToolRole in ProjectRelevant PackagesounddeviceCore I/OUsed for efficient, cross-platform, non-blocking audio stream input (NumPy arrays).librosa / aubioFeature ExtractionPrimary libraries for real-time analysis: aubio is preferred for robust real-time pitch detection.Data Contract (Output Stream): A sequence of dictionaries representing a chunk of audio features.Python# Stream Dict Example
{
    'timestamp': float,       # Time of the analysis window center
    'pitch_hz': float,        # Fundamental frequency (e.g., 440.0)
    'pitch_confidence': float,# Confidence score for the pitch detection (0.0 to 1.0)
    'loudness_db': float,     # RMS energy converted to dB
    'spectral_centroid_norm': float, # Normalized measure of tonal brightness (0.0 to 1.0)
    'is_voiced': bool         # Indicates if a pitch was confidently detected
}
Testing Plan:Dual-Process Test (Source $\rightarrow$ Extractor):Tool Usage: Use qh.testing.serve_app to launch the AUDIO_FEATURER service.Procedure:Process A (Source): Write a generator that synthesizes a short sine wave sweep (e.g., 440 Hz to 880 Hz) using sounddevice's output capabilities.Process B (Extractor): The AUDIO_FEATURER service listens to this internal loopback audio (or a mock audio stream pipe).Verification: The test should verify that the output stream's pitch_hz value tracks the input sine wave sweep with high accuracy, ensuring the real-time pitch detection works as expected.Output Stream Projects (Consuming Control Streams)Project 5: SYNTH_HOOKProject Name: SYNTH_HOOKAlternative Name Candidate: synthiumFunction: An independent Python synthesizer wrapper that consumes a control stream (dict of DSP parameters) and converts it into a continuous audio stream using a powerful backend, providing an alternative to the project's default hum/pd4web solution.ToolRole in ProjectRelevant Packagepyo or ctcsoundCore DSP Enginepyo is recommended for its pure-Python API for signal processing and real-time parameter manipulation.sounddeviceAudio SinkUsed internally by the synth wrapper to manage low-latency audio playback.Data Contract (Input Stream - Control Vector): A high-rate sequence of dictionaries.Python# Control Stream Dict Example (Input)
{
    'timestamp': float,          
    'pitch_hz': float,           # The fundamental frequency to play (e.g., 440.0)
    'amplitude': float,          # Main volume (0.0 to 1.0)
    'filter_cutoff_hz': float,   # Dynamic filter parameter
    'waveform_type': str         # e.g., 'sine', 'saw', 'square'
}
Testing Plan:Dual-Process Test (Stream $\rightarrow$ Sound): This test verifies that the synthium package can consume control dicts and produce audio.Tool Usage: Use qh.testing.serve_app to launch the SYNTH_HOOK service.Procedure:Process A (Source): A service generates a stream of control dicts (e.g., a sequence that smoothly sweeps the pitch_hz from 220 to 880 over 5 seconds).Process B (Sink): The SYNTH_HOOK service consumes this stream and drives the pyo engine's parameters.Verification: Since direct audio verification is hard in CI, the test must verify the real-time parameter update API is called correctly by the SYNTH_HOOK wrapper when a new dict is received. Additionally, provide instructions to the agent to include a PyO test using a mock audio output stream (instead of actual speakers) and verify the resulting audio buffer array contents (e.g., using scipy.fft to confirm the expected frequency peak).Project 6: ACCOMPANIMENT_ENGINEProject Name: ACCOMPANIMENT_ENGINEAlternative Name Candidate: ym (Your Modulator/Mixer)Function: An engine for AI/Algorithmic music generation that takes the user's control stream as input (like a suggested key/tempo) and outputs a secondary control stream (e.g., bassline, rhythmic pattern) for accompaniment. Focus on live, steerable generation.ToolRole in ProjectRelevant PackageLyria RealTime APICore AI GenerationUtilizes a state-of-the-art, real-time, streaming music generation model to provide live, steered accompaniment.midoUtilitiesHandles MIDI format conversion for easier integration into traditional DAWs or other synths.i2 (Image Generation)UI/UX Asset GenerationMandated to generate high-quality, abstract visual assets for a hypothetical control panel or visualization of the generated music (e.g., a 'rhythm grid' visualization).Data Contract (Input Stream - User Control): High-level data from the wide pipeline.Python# User Control Stream Dict (Input)
{
    'timestamp': float,
    'current_pitch_hz': float,
    'current_scale_key': str,    # e.g., 'C major', derived from pitch history
    'tempo_bpm': int             # Derived from input stream rhythm or set by keyboard
}
Data Contract (Output Stream - AI Control): A sequence of dictionaries representing generated musical events.Python# AI Control Stream Dict (Output - MIDI-like)
{
    'timestamp': float,
    'midi_note': int,            # Generated note pitch
    'velocity': int,             # Note volume/emphasis
    'duration': float,           # Note length
    'instrument': str            # e.g., 'bassline', 'drums'
}
Testing Plan:End-to-End Generative Test:Tool Usage: Use qh.testing.run_app to launch the engine service in an integration test mode.Procedure:Process A (Source): A mock client sends a continuous stream of {'current_scale_key': 'C major', 'tempo_bpm': 120}.Process B (Sink): The ACCOMPANIMENT_ENGINE uses this input to call the Lyria RealTime API (or mock the API call) and outputs the resulting generated notes.Verification: The test must verify two things:API Interaction: The service successfully translates the input dict into the correct API format for the AI model.Musical Correctness: The output stream notes (midi_note) are algorithmically checked using a library like music21 to ensure they fall within the requested C major scale, confirming the engine's control logic functions correctly.Project 7: AUDIO_OUTPUT_BRIDGEProject Name: AUDIO_OUTPUT_BRIDGEAlternative Name Candidate: oohFunction: A minimalist, low-latency utility package responsible only for taking raw, time-series audio buffers (NumPy arrays) generated by a synthesis engine (like synthium) and reliably pushing them to the system's sound card. This is the final, dedicated audio I/O layer.ToolRole in ProjectRelevant PackagesounddeviceCore I/OThe preferred choice for robust, high-performance, asynchronous audio playback using NumPy arrays.NumPyData StructureExplicitly mandate the use of NumPy arrays for all audio buffers to ensure performance.Data Contract (Input Stream - Raw Audio Buffer): A sequence of NumPy arrays streamed for immediate playback.Python# Raw Audio Buffer Stream (Input)
# This stream is a sequence of audio frames.
{
    'timestamp': float,
    'buffer': np.ndarray,      # 1D or 2D NumPy array of audio data (e.g., float32, mono or stereo)
    'samplerate': int          # The sample rate of the buffer (e.g., 44100)
}
Testing Plan:Dual-Process Test (Buffer $\rightarrow$ Playback):Tool Usage: Use qh.testing.serve_app to launch the AUDIO_OUTPUT_BRIDGE service.Procedure:Process A (Source): A generator creates a sequence of NumPy arrays containing a short, known sine wave burst (e.g., 100ms of 1000 Hz tone).Process B (Sink): The AUDIO_OUTPUT_BRIDGE service consumes these buffers and sends them to a sounddevice stream.Verification: The test should not verify auditory output directly, but rather confirm that the core sounddevice stream write callback function is called successfully with the incoming buffers and that the service reports zero buffer overruns or underruns, verifying the stability and low-latency nature of the bridge.